var myAge = 15;

/* if (myAge >= 18&& myAge <= 30) <- is inversie */
 

if (myAge < 18 || myAge > 30){
    document.write("Nope")
}
else{
    document.write("je mag komen")
}

/* && en (and) */

/* || of (or) */